# REQ-2026-003 Ship Report

## 发布内容

P1 任务实施的 4 个功能：

1. **ChromaDB 维度警告** - 设置页嵌入模型配置显示
2. **SSE 重连机制** - 网络不稳定时的自动重连
3. **可观测性仪表盘** - LLM 成本监控
4. **SDK 生成** - Python/TypeScript SDK

---

## 变更文件清单

### 前端

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `frontend/src/views/SettingsView.vue` | 修改 | 添加嵌入模型配置卡片 |
| `frontend/src/composables/useSSEStream.ts` | 修改 | 添加重连机制 |
| `frontend/src/composables/chat/useChatSSE.ts` | 修改 | 重构使用统一 SSE |
| `frontend/src/composables/workflow/useWorkflowExecution.ts` | 修改 | 启用重连参数 |
| `frontend/src/views/AdminDashboardView.vue` | 新增 | 管理仪表盘页面 |
| `frontend/src/composables/useDashboard.ts` | 新增 | 仪表盘 API 调用 |
| `frontend/src/router/index.ts` | 修改 | 添加仪表盘路由 |

### 后端

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `backend/app/api/observability.py` | 新增 | Token 使用 API |
| `backend/app/api/settings.py` | 修改 | 添加嵌入模型配置 API |
| `backend/main.py` | 修改 | 注册 observability router |

### SDK

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `scripts/generate-sdk.sh` | 新增 | SDK 生成脚本 |
| `sdks/python/` | 新增 | Python SDK |
| `sdks/typescript/` | 新增 | TypeScript SDK |

---

## 发布检查清单

- [x] 前端构建通过
- [x] 后端语法检查通过
- [x] Code Review 完成
- [x] QA 测试完成
- [ ] 用户手动验证

---

## 风险提示

1. **SSE 重连**: 首次上线，建议观察日志确认重连行为正常
2. **可观测性 API**: 需要确保 `token_usage.log` 文件存在且有写入权限
3. **SDK**: 当前为手写客户端，后续可切换到 OpenAPI Generator

---

## 回滚方案

```bash
# 如需回滚
git revert <commit-hash>
```

---

## 后续工作

- [ ] P2 任务（审查文档中的中优先级项）
- [ ] 手动验证仪表盘数据展示
- [ ] 完善 SDK 自动生成流程

---
*发布时间: 2026-03-29*
*发布人: Claude Code*
